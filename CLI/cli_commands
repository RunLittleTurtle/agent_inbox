# Install new dependencies first
pip install -r requirements.txt

# Check service status
python cli.py status

# 🚀 START EVERYTHING (One Command!)
python cli.py start                   # Starts both LangGraph server + Agent Inbox UI (use for agent inbox : http://localhost:2024)

python cli.py start --langgraph-port 2025 --inbox-port 3001  # Custom other ports

# Launch individual services
python cli.py langgraph               # Launch LangGraph dev server only
python cli.py inbox                   # Launch Agent Inbox UI only

# Create and test email workflow
python cli.py email

# Get help for any command
python cli.py --help
python cli.py email --help

# Setup Gmail OAuth authentication (REQUIRED FIRST TIME)
python cli.py setup-oauth               # Setup Google OAuth for Gmail access

# Fetch latest Gmail emails and trigger workflow
python cli.py gmail                     # Fetch 1 latest email and send to main agent workflow
python cli.py gmail --count 5           # Fetch 5 latest emails and send to main agent workflow
python cli.py gmail --executive         # Fetch 1 latest email and send to executive-ai-assistant workflow
python cli.py gmail --executive --count 3  # Fetch 3 latest emails and send to executive-ai-assistant workflow



# Clean up auto-configured inboxes (run in browser console on Agent Inbox page)
# Copy and paste the contents of cleanup_inboxes.js into browser console at http://localhost:3000

# venv
source .venv/bin/activate

git add .
git commit -m "comment"
git push origin main

agent-inbox info :
Mutli-Agent: id -> agent / url -> http://localhost:2024
Executive-ai:id -> main / url -> http://localhost:2025


# Configuration UI Management
python cli.py config                    # Launch Configuration UI on port 3004
python cli.py config --clean            # Clear cache and launch Configuration UI
python cli.py config --port 3005       # Launch Configuration UI on custom port
python cli.py clear-cache              # Clear Next.js cache (fixes styling issues)

# Manual cache clearing for debug (run in agent-inbox directory)
When you get cache errors:
  npm run clear-cache
  npm run dev:config

  Or one command:
  npm run dev:config:clean

  Or use the CLI:
  python cli.py clear-cache
  python cli.py config --clean
